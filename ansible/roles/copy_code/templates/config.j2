{
    "client_id": "{{ mqtt_client_id }}",
    "server": "{{ mqtt_broker_address }}",
    "port": {{ mqtt_broker_port | default('1883') }},
    "topic": "{{ mqtt_publish_topic }}",
    "ssid": "{{ ssid }}",
    "wifi_pw": "{{ ssid_password }}",
    "ntp_server": "{{ ntp_server | default('10.0.1.201') }}",
    "sensor_type": "{{ sensor_type | default('dht22') }}",
    "sda_pin": {{ boards[board].sda_pin }},
    "scl_pin": {{ boards[board].scl_pin }},
    "disable_watchdog": {{ disable_watchdog | default(false) | bool | to_nice_json }},
    "enable_bme280_additional_data": {{ enable_bme280_additional_data | default(true) | bool | to_nice_json }}
}
